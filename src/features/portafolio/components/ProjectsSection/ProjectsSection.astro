---
import { Projects } from "./components/Projects";
import { SectionTitle } from "@/shared/components/SectionTitle/SectionTitle";
import AnimatedContainer from "@/shared/components/animated/AnimatedContainer";
---

<section
  class="w-full bg-linear-to-b from-transparent via-purple-500/2 to-transparent"
>
  <div class="w-full container mx-auto p-4">
    <SectionTitle title="Proyectos:" client:only />

    <div class="flex flex-col text-sm my-3 px-4 lg:px-7 md:px-0 gap-5 w-full">
      <AnimatedContainer client:load once={false}>
        <p class="lg:text-lg lg:py-5">
          Desde la arquitectura hasta el despliegue: soluciones donde la
          robustez técnica resuelve problemas operativos reales.
        </p>
      </AnimatedContainer>
      <Projects client:load />
    </div>
  </div>
</section>
